(define matriz (vector (make-vector 4) (make-vector 4) (make-vector 4) (make-vector 4)))
(define (movimientoArriba)

    (define (ciclo1)

    )
)
(define (opera-pares n n2)
    (cond ((= n n2) consequent1)
        (predicate2 consequent2))
)